import { TODOElement } from "../elements/types";

const useCurrentElement = () => {
  const element: TODOElement = {} as unknown as TODOElement;
  return element;
};

export default useCurrentElement;
